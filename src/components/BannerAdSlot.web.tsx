// ─── BannerAdSlot — web stub ────────────────────────────────────────────────
//
// react-native-google-mobile-ads is a native module with no web support.
// Metro resolves this file instead of BannerAdSlot.tsx when bundling for
// web, so the native ad SDK's JS is never pulled into the web bundle —
// same mechanism as ads.web.ts.
//
// ─────────────────────────────────────────────────────────────────────────────

import React from 'react';

export const BannerAdSlot: React.FC = () => null;

export default BannerAdSlot;
